---
id: c3-210
c3-seal: c024e4dd4862f98bc6b8e3926671c0916d0673f6ef8c51e306934199eb245f2e
title: init
type: component
parent: c3-2
goal: |-
    Stamp a brand-new wiki instance from scratch — scaffold, seed, hooks wired, first commit made —
    so a fresh wiki is immediately lint-clean and ready to ingest, in one `python3
    wiki-harness/init.py <target-dir> [flags]` invocation.
uses:
    - ref-ownership-classes
    - rule-pure-core-impure-edge
    - rule-stdlib-only-py39
---

## Goal

Stamp a brand-new wiki instance from scratch — scaffold, seed, hooks wired, first commit made —
so a fresh wiki is immediately lint-clean and ready to ingest, in one `python3
wiki-harness/init.py <target-dir> [flags]` invocation.

## Parent Fit

| Field | Value |
| --- | --- |
| Container | c3-2 (lifecycle) |
| Category | Feature — the business flow that assembles c3-201 (manifest), c3-3's three template components, and c3-1's hooks into one working wiki instance |
| Depends on | c3-201 (manifest, step 10 write); c3-301/c3-302/c3-303 (templates container, steps 6-9 render/copy/seed); c3-1 (scripts, step 5 copy verbatim, steps 12-13 self-verify by running them) |
| Depended on by | Nothing inside c3-2; c3-211 (upgrade)'s scratch-copy render logic (step 9) reuses the same per-role copy/render behaviour this component defines for a fresh stamp |

## Purpose

Own `init.py`'s 16 ordered, fail-closed steps (plan-v3 §3.1): resolve target, collect the
template variables — only `wiki_title` is required, the other three are derived from the
target's basename and the library defaults by the pure `apply_defaults()` — plus `--origins`,
`git init`, create the three `.gitkeep` placeholders, copy scripts/hooks verbatim, render
`AGENTS.md`/`README.md`, copy MANAGED files verbatim, seed SEEDED files, seed the 4 tracked
`CLAUDE.md` stubs, write the manifest, wire `core.hooksPath`, self-verify by actually running
`lint.py` and both hooks, make the real first commit, verify it landed, print the summary.
Non-goal: `init` never runs again against an already-initialized wiki — that is `c3-211`
(upgrade)'s job entirely; the two share `c3-201`/`c3-3` and, since v1.2.0, `apply_defaults()`
on the `--adopt` path, but not each other's flow.

## Governance

| Reference | Type | Governs | Precedence | Notes |
| --- | --- | --- | --- | --- |
| rule-pure-core-impure-edge | rule | Every ordered step that decides pass/fail (e.g. "is the target non-empty," "does the manifest hash match") is a pure check over already-collected data; every filesystem write, git/subprocess call, and interactive prompt is a named edge | Hard | Same split c3-201/c3-211 follow for their own pure/impure boundary |
| rule-stdlib-only-py39 | rule | init.py imports only the standard library, opens with from __future__ import annotations | Hard | Same floor every module in scripts/lifecycle targets |
| ref-ownership-classes | ref | Steps 6-9 place TEMPLATE/MANAGED/SEEDED bytes exactly per that ref's class rules | Hard | This component is where the ref's three library-owned classes actually get placed on disk for the first time |

## Contract

| Surface | Direction | Contract | Boundary | Evidence |
| --- | --- | --- | --- | --- |
| python3 wiki-harness/init.py <target-dir> --wiki-title <title> [--org-name ...] [--content-language ...] [--repo-name ...] [--origins a,b,c] [--answers-file ...] [--non-interactive] [--force] | IN/OUT | Fail-closed at any of the 16 ordered steps. `--wiki-title` is the only required variable (v1.2.0); `org_name`, `content_language`, and `repo_name` are derived when omitted — from `wiki_title`, `English`, and the target's resolved basename — and a supplied value is never overridden. A non-empty target without --force exits 2 with the exact quoted message before writing anything; a missing `--wiki-title` under --non-interactive exits 2 naming the flag; a prompt with no readable stdin exits 2 naming --non-interactive, never a traceback. Success leaves one real commit, a clean git status, and lint.py --root . exiting 0 | CLI process boundary | plan-v3.md §3.1; CHANGELOG.md v1.2.0; docs/compatibility-policy.md §2.1 |
| Post-condition self-check | OUT | Before declaring success, init itself re-runs lint.py --root <target> and dry-runs both hooks as real subprocesses (steps 12-13) — a scaffold is never left in a state its own linter would reject | Impure edge — subprocess invocation of c3-1's scripts | plan-v3.md §3.1 steps 12-13 |
| No tests/ directory | OUT | A freshly-scaffolded target never contains a tests/ folder, at any step | Filesystem output contract | plan-v3.md §3.1 Post-conditions (A6) |

## Derived Materials

| Material | Must derive from | Allowed variance | Evidence |
| --- | --- | --- | --- |
| wiki-harness/init.py | This component's own Contract surfaces. plan-v3.md §3.1's 16 ordered steps and quoted failure messages are the oracle for everything the extraction shipped; every delta after v1.0.0 is governed instead by docs/compatibility-policy.md §2 and recorded with its release type in CHANGELOG.md | Step ordering fixed. Message text fixed for every message plan-v3 §3.1 quotes; a NEW message or a narrowed required-variable set is permitted where the policy's §2 row for init.py allows it and the CHANGELOG entry states the release type | plan-v3.md §7 Phase 2 (T13/T14); docs/compatibility-policy.md §2.1 |
| tests/test_init.py | This component's Contract's post-condition surfaces, including the tracked-CLAUDE.md and source_url assertions | Test framing may vary; asserted post-conditions may not | plan-v3.md §3.1 (Post-conditions), §7 (T13) |
