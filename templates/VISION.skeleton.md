# VISION — what the wiki is not doing yet

Everything consciously postponed lives here, so a deferral is a decision with a status
rather than something quietly forgotten. Add an entry the moment work is deferred.

Status is one of: `deferred` · `active` · `done` · `dropped`.
Every entry states what unblocks it — a status with no unblock condition cannot be acted on.
