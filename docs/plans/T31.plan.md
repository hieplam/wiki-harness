# Plan — card `T31`

Thin plan. The buildable detail for the task lives in that task's brief, named in the task
section below; this file exists so the campaign runner and `validate-plan.sh` have a
mechanically well-formed plan, and so the acceptance commands are in one place. Do not restate
a brief here and do not treat this file as a substitute for reading one.

## Global Constraints

The implementer is the `hunter` subagent. Dispatch one hunter subagent per task and never
write the feature source in the orchestrating session itself. Every deliverable is then
audited by two independent `skinner` subagents that run the proof, and the diff is reviewed by
a `tracker` subagent before the card's final commit.

Purity: core logic stays deterministic and free of side effects; every outside-world
dependency — the filesystem, a subprocess, the network, the clock, randomness, global state —
enters through a named edge the caller supplies, and is never constructed inside a decision
function. See `~/.claude/rules/pure-core.md`.

All nine standing rules, amendment A12, the commit trailers, the branching and pull-request
convention, the audit roster and the settled decisions are in
`docs/plans/CAMPAIGN-CONSTRAINTS.md`. That file governs this plan and the task in it.

### Task 2: T31 — init a throwaway synthetic ai-wiki fixture; run the full E2E including a customized id.pattern

Requirement contract: `/Users/hip/repo/wiki-harness-analysis/plan/briefs/T31.md` — read it in full, including any trailing
section headed "Warchief amendment". Work on branch `task/T31-ai-wiki-smoke`, following the branching
convention in the campaign constraints file.

Cut the branch from `main`. Tick this task's own `docs/PLAN.md` box in the same commit as the
code, and nobody else's.

The fixture lives in /tmp and is NEVER created inside /Users/hip/repo/. It is never pushed to a real remote and never becomes a kept repository -- that is T32's job, which is explicitly optional and owner-timed. Confirm the fixture has no `tests/` directory (A6) as part of the verification.

SHAMAN RULING R4 (2026-09-02): this card's FIRST attempt correctly escalated rather than shipping -- its own verbatim acceptance command crashed on a real defect in the released v1.0.0 (`init.py`'s `dry_run_hooks` execed a target-relative hook path under `cwd=target`, so a relative target doubled). That defect is card T31A, which runs immediately before this one. This card's acceptance commands are UNCHANGED and are re-run verbatim against the fixed library. The brief says the fixture is initialised 'from wiki-harness v1.0.0'; read that as 'from the release on `main`', which is v1.0.1 once T31A ships. Record the actual version in the evidence record. If the acceptance command still fails, escalate again -- do NOT switch to an absolute target to make it pass.

Amendment A12 (owner-ratified 2026-09-02): this card's product is a throwaway /tmp fixture and it creates no tracked file on its own, so it additionally commits an evidence record at `docs/second-wiki-smoke.md`. The record states each command that was run verbatim, what was observed, and the verdict -- including both halves of the card: the default-schema E2E AND the customized `^ai-\d{4}-\d{2}-\d{2}-\d{3}$` id.pattern half, which is the direct proof that a hardcoded `src-` prefix no longer blocks a differently-configured second wiki. The record is a record, not a new requirement: it does not change this card's scope, and the brief's acceptance commands remain the only gate.

Acceptance commands, run verbatim in a fresh shell:

```sh
cd /tmp && python3 /Users/hip/repo/wiki-harness/init.py ai-wiki-smoke --wiki-title 'AI Wiki' --org-name 'AI tribe' --content-language English --repo-name ai-wiki --non-interactive
cd /tmp/ai-wiki-smoke && python3 scripts/lint.py
test -f /Users/hip/repo/wiki-harness/docs/second-wiki-smoke.md
cd /Users/hip/repo/wiki-harness && python3 -m unittest discover -s tests -q
```

Expected: every command above exits 0 — except any this plan explicitly names as an inspection
rather than a gate — and the full suite (`python3 -m unittest discover -s tests -q` from the
repository root) stays green. Re-run all of them yourself before claiming the task is done.

- [ ] **Step 1: Commit** — one or more conventional commits, each ending with the three
      trailers from the campaign constraints file, using `Tribe-Task: 2/5`,
      and never a Co-Authored-By trailer. Tick only this task's own `docs/PLAN.md` box, in the
      same commit as the code.
