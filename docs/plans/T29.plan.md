# Plan — card `T29`

Thin plan. The buildable detail for the task lives in that task's brief, named in the task
section below; this file exists so the campaign runner and `validate-plan.sh` have a
mechanically well-formed plan, and so the acceptance commands are in one place. Do not restate
a brief here and do not treat this file as a substitute for reading one.

## Global Constraints

The implementer is the `hunter` subagent. Dispatch one hunter subagent per task and never
write the feature source in the orchestrating session itself. Every deliverable is then
audited by two independent `skinner` subagents that run the proof, and the diff is reviewed by
a `tracker` subagent before the card's final commit.

Purity: core logic stays deterministic and free of side effects; every outside-world
dependency — the filesystem, a subprocess, the network, the clock, randomness, global state —
enters through a named edge the caller supplies, and is never constructed inside a decision
function. See `~/.claude/rules/pure-core.md`.

All nine standing rules, amendment A12, the commit trailers, the branching and pull-request
convention, the audit roster and the settled decisions are in
`docs/plans/CAMPAIGN-CONSTRAINTS.md`. That file governs this plan and the task in it.

### Task 5: T29 — run the baseline oracle against the migrated clone and diff it against before/

Requirement contract: `/Users/hip/repo/wiki-harness-analysis/plan/briefs/T29.md` — read it in full, including any trailing
section headed "Warchief amendment". Work on branch `task/T29-baseline-oracle-diff`, following the branching
convention in the campaign constraints file.

Cut the branch from `main`. Tick this task's own `docs/PLAN.md` box in the same commit as the
code, and nobody else's.

SHAMAN RULING R1, settled up front so it is never escalated: the second acceptance command, the `diff -r`, is an INSPECTION, not an exit-code gate -- the brief's own acceptance section says so ('or produce the described output where the command is inspection, not a gate'). Two of the three pre-approved deltas are real content differences, so `diff` exiting non-zero is EXPECTED and is not a failure. The gate is the CONTENT of the diff: it must contain exactly these three deltas and nothing else -- (1) meta/env.txt's git_head lines, already excluded by --exclude=env.txt; (2) lint/exit.txt going 1 -> 0, because --adopt finally configures core.hooksPath; (3) tests/stdout.txt changing from unittest's own output to the fixed TEST_CMD override line, with tests/exit.txt staying 0 and tests/stderr.txt staying empty on both sides. ANY other differing file anywhere in before/ or after/ -- in particular any new finding in lint/stdout.txt or card-lint/stdout.txt, or any changed commit-message verdict -- is a regression and BLOCKS the card. Report the full diff output in the evidence record either way.

Amendment A12 (owner-ratified 2026-09-02): this card's product is `baseline/after/` in the analysis directory, not in this repository, so it APPENDS its verdict to the evidence record `docs/migration-record.md` that T28 created. The appended section names every delta the diff actually produced and states, delta by delta, which of the three pre-approved ones it is. The record is a record, not a new requirement: it does not change this card's scope, and the brief's acceptance commands remain the only gate.

Acceptance commands, run verbatim in a fresh shell:

```sh
TEST_CMD='echo "tests/ removed by migration - all wiki-harness tests now live in the wiki-harness library repo"; exit 0' bash /Users/hip/repo/wiki-harness-analysis/baseline/run.sh /Users/hip/repo/wiki-harness-analysis/ogp-wiki-clone /Users/hip/repo/wiki-harness-analysis/baseline/after
diff -r --exclude=env.txt /Users/hip/repo/wiki-harness-analysis/baseline/before /Users/hip/repo/wiki-harness-analysis/baseline/after
test -f /Users/hip/repo/wiki-harness/docs/migration-record.md
cd /Users/hip/repo/wiki-harness && python3 -m unittest discover -s tests -q
```

Expected: every command above exits 0 — except any this plan explicitly names as an inspection
rather than a gate — and the full suite (`python3 -m unittest discover -s tests -q` from the
repository root) stays green. Re-run all of them yourself before claiming the task is done.

- [ ] **Step 1: Commit** — one or more conventional commits, each ending with the three
      trailers from the campaign constraints file, using `Tribe-Task: 5/5`,
      and never a Co-Authored-By trailer. Tick only this task's own `docs/PLAN.md` box, in the
      same commit as the code.
