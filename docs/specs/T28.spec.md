# Spec — card `T28`

**Campaign:** wiki-harness-migration | **Phase:** P4 — Migrate ogp-wiki | **Depends on:** `T27`

## What this card delivers

T28 — delete tests/ entirely from the ogp-wiki clone.

This spec is a thin pointer, not a restatement. The requirement contract for the task below
is its brief, listed with the task. Read the brief in full, including any trailing section
headed "Warchief amendment", before writing a line of code.

## Tasks in this card

1. **T28** — T28 — delete tests/ entirely from the ogp-wiki clone
   - Brief (the real contract): `/Users/hip/repo/wiki-harness-analysis/plan/briefs/T28.md`
   - Task branch: `task/T28-delete-tests-dir`
   - Campaign task number: 3 of 4

## Evidence record

Amendment A12 (owner-ratified 2026-09-02): this card's product lands in the disposable clone, not in this repository, so it additionally CREATES the evidence record at `docs/migration-record.md`. The record covers the migration so far -- what `--adopt` wrote in T27, and what this card removed -- with the verbatim commands and the observed result. T29 appends the oracle verdict to the same file. The record is a record, not a new requirement: it does not change this card's scope, and the brief's acceptance commands remain the only gate.

## Scope fence

- Only the task listed above may produce a change. Nothing else in the repository is touched.
- `/Users/hip/repo/ogp-wiki`, the live wiki, is never written to. Not one byte.
- No CI workflow file is created, in this card or any other.
- Build nothing beyond the brief; escalate an ambiguity instead of guessing at it.

`git rm -r tests/` in the clone, one commit. This is a deletion outright, not a prune-and-add-a-golden-test -- v2's plan was replaced by A6, and no wiki instance ever carries a tests/ directory. This MUST run before T29's baseline-oracle capture so the after-snapshot reflects the final state; v2's ordering left that circular and v3 fixed it.

## Governing constraints

`docs/plans/CAMPAIGN-CONSTRAINTS.md` applies in full: the nine standing rules, amendment A12,
the commit trailers, the branching and pull-request convention, the mandatory audit roster of
one hunter, two independent skinners and one tracker, and the settled decisions that are never
reopened.

## Definition of done

Every acceptance command in this card's plan exits 0 when re-run in a fresh shell (except any
the plan explicitly names as an inspection rather than a gate), both skinners report no
confirmed gating finding, the tracker approves the diff, the pull request into `main` is
merged, and `docs/PLAN.md` carries this card's task ticked.
