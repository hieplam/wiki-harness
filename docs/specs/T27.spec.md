# Spec — card `T27`

**Campaign:** wiki-harness-migration | **Phase:** P4 — Migrate ogp-wiki | **Depends on:** `T31`

## What this card delivers

T27 — write upgrade.py's --adopt code path and run the migration on a disposable ogp-wiki clone.

This spec is a thin pointer, not a restatement. The requirement contract for the task below
is its brief, listed with the task. Read the brief in full, including any trailing section
headed "Warchief amendment", before writing a line of code.

## Tasks in this card

1. **T27** — T27 — write upgrade.py's --adopt code path and run the migration on a disposable ogp-wiki clone
   - Brief (the real contract): `/Users/hip/repo/wiki-harness-analysis/plan/briefs/T27.md`
   - Task branch: `task/T27-adopt-migration`
   - Campaign task number: 3 of 5

## Evidence record

This card needs no A12 evidence record: it writes `upgrade.py`'s `--adopt` code path itself, which is a library change and therefore already a real pull request into `main`. Read the brief's own paragraph on this -- the `--adopt` code path is IMPLEMENTATION WORK belonging to this card, layered on the phase-3 `upgrade.py` built by T16B through T25. This card is not merely invoking a pre-built flag.

## Scope fence

- Only the task listed above may produce a change. Nothing else in the repository is touched.
- `/Users/hip/repo/ogp-wiki`, the live wiki, is never written to. Not one byte.
- No CI workflow file is created, in this card or any other.
- Build nothing beyond the brief; escalate an ambiguity instead of guessing at it.

Create the disposable clone first if it does not exist: `git clone /Users/hip/repo/ogp-wiki /Users/hip/repo/wiki-harness-analysis/ogp-wiki-clone`. Every step of this card operates ONLY on that clone. The live /Users/hip/repo/ogp-wiki is never written to -- standing rule 5, and an owner-only escalation trigger. The one hand-authored, non-mechanical step is removing the CLAUDE.md line from the clone's .gitignore before the 4 tracked CLAUDE.md files are seeded; everything else in the migration is mechanical. Do NOT delete tests/ here -- that is T28, and the ordering matters.

## Governing constraints

`docs/plans/CAMPAIGN-CONSTRAINTS.md` applies in full: the nine standing rules, amendment A12,
the commit trailers, the branching and pull-request convention, the mandatory audit roster of
one hunter, two independent skinners and one tracker, and the settled decisions that are never
reopened.

## Definition of done

Every acceptance command in this card's plan exits 0 when re-run in a fresh shell (except any
the plan explicitly names as an inspection rather than a gate), both skinners report no
confirmed gating finding, the tracker approves the diff, the pull request into `main` is
merged, and `docs/PLAN.md` carries this card's task ticked.
