# Spec — card `T29`

**Campaign:** wiki-harness-migration | **Phase:** P4 — Migrate ogp-wiki | **Depends on:** `T28`

## What this card delivers

T29 — run the baseline oracle against the migrated clone and diff it against before/.

This spec is a thin pointer, not a restatement. The requirement contract for the task below
is its brief, listed with the task. Read the brief in full, including any trailing section
headed "Warchief amendment", before writing a line of code.

## Tasks in this card

1. **T29** — T29 — run the baseline oracle against the migrated clone and diff it against before/
   - Brief (the real contract): `/Users/hip/repo/wiki-harness-analysis/plan/briefs/T29.md`
   - Task branch: `task/T29-baseline-oracle-diff`
   - Campaign task number: 5 of 5

## Evidence record

Amendment A12 (owner-ratified 2026-09-02): this card's product is `baseline/after/` in the analysis directory, not in this repository, so it APPENDS its verdict to the evidence record `docs/migration-record.md` that T28 created. The appended section names every delta the diff actually produced and states, delta by delta, which of the three pre-approved ones it is. The record is a record, not a new requirement: it does not change this card's scope, and the brief's acceptance commands remain the only gate.

## Scope fence

- Only the task listed above may produce a change. Nothing else in the repository is touched.
- `/Users/hip/repo/ogp-wiki`, the live wiki, is never written to. Not one byte.
- No CI workflow file is created, in this card or any other.
- Build nothing beyond the brief; escalate an ambiguity instead of guessing at it.

SHAMAN RULING R5 SUPERSEDES R1 (owner-ratified 2026-09-03; plan-v3 appendix A13). R1 stated this gate as 'exactly three deltas' and then named three FILES. That was wrong and made the card unpassable: each approved cause touches several captured files, and two further causes were never enumerated. The first attempt at this card escalated rather than rationalising the extra deltas away, and was right to.

THE GATE NOW: the migrated clone passes if and only if BOTH hold -- (1) every differing file between baseline/before/ and baseline/after/ is attributable to one of the five causes C1-C5 enumerated in plan-v3 appendix A13 and in answers.md ruling R5, and (2) `card-lint/*` and `commit-msg/*` -- the corpora that measure ogp-wiki's own real content -- are BYTE-IDENTICAL. Read R5 for the full file-by-file enumeration; it lists all 36 differing files against their cause.

The `diff -r` acceptance command is an INSPECTION, not an exit-code gate -- its non-zero exit is expected, and the brief's own acceptance section allows for this ('or produce the described output where the command is inspection, not a gate').

STILL BLOCKING: any differing file NOT attributable to C1-C5; any new finding on ogp-wiki's real unmutated content; a changed exit code or output ordering outside those causes; a changed commit-message verdict on the corpus.

DELIVERABLE: attribute every differing file to its cause in `docs/migration-record.md`, state explicitly that `card-lint/*` and `commit-msg/*` are byte-identical, and record any file you cannot attribute. Do NOT edit the baseline scenarios, do NOT re-capture `before/`, and do NOT touch `--adopt` -- the owner considered and rejected all three on 2026-09-03.

## Governing constraints

`docs/plans/CAMPAIGN-CONSTRAINTS.md` applies in full: the nine standing rules, amendment A12,
the commit trailers, the branching and pull-request convention, the mandatory audit roster of
one hunter, two independent skinners and one tracker, and the settled decisions that are never
reopened.

## Definition of done

Every acceptance command in this card's plan exits 0 when re-run in a fresh shell (except any
the plan explicitly names as an inspection rather than a gate), both skinners report no
confirmed gating finding, the tracker approves the diff, the pull request into `main` is
merged, and `docs/PLAN.md` carries this card's task ticked.
