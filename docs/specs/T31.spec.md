# Spec — card `T31`

**Campaign:** wiki-harness-migration | **Phase:** P5 — Second-wiki smoke | **Depends on:** `T31A`

## What this card delivers

T31 — init a throwaway synthetic ai-wiki fixture; run the full E2E including a customized id.pattern.

This spec is a thin pointer, not a restatement. The requirement contract for the task below
is its brief, listed with the task. Read the brief in full, including any trailing section
headed "Warchief amendment", before writing a line of code.

## Tasks in this card

1. **T31** — T31 — init a throwaway synthetic ai-wiki fixture; run the full E2E including a customized id.pattern
   - Brief (the real contract): `/Users/hip/repo/wiki-harness-analysis/plan/briefs/T31.md`
   - Task branch: `task/T31-ai-wiki-smoke`
   - Campaign task number: 2 of 5

## Evidence record

Amendment A12 (owner-ratified 2026-09-02): this card's product is a throwaway /tmp fixture and it creates no tracked file on its own, so it additionally commits an evidence record at `docs/second-wiki-smoke.md`. The record states each command that was run verbatim, what was observed, and the verdict -- including both halves of the card: the default-schema E2E AND the customized `^ai-\d{4}-\d{2}-\d{2}-\d{3}$` id.pattern half, which is the direct proof that a hardcoded `src-` prefix no longer blocks a differently-configured second wiki. The record is a record, not a new requirement: it does not change this card's scope, and the brief's acceptance commands remain the only gate.

## Scope fence

- Only the task listed above may produce a change. Nothing else in the repository is touched.
- `/Users/hip/repo/ogp-wiki`, the live wiki, is never written to. Not one byte.
- No CI workflow file is created, in this card or any other.
- Build nothing beyond the brief; escalate an ambiguity instead of guessing at it.

The fixture lives in /tmp and is NEVER created inside /Users/hip/repo/. It is never pushed to a real remote and never becomes a kept repository -- that is T32's job, which is explicitly optional and owner-timed. Confirm the fixture has no `tests/` directory (A6) as part of the verification.

SHAMAN RULING R4 (2026-09-02): this card's FIRST attempt correctly escalated rather than shipping -- its own verbatim acceptance command crashed on a real defect in the released v1.0.0 (`init.py`'s `dry_run_hooks` execed a target-relative hook path under `cwd=target`, so a relative target doubled). That defect is card T31A, which runs immediately before this one. This card's acceptance commands are UNCHANGED and are re-run verbatim against the fixed library. The brief says the fixture is initialised 'from wiki-harness v1.0.0'; read that as 'from the release on `main`', which is v1.0.1 once T31A ships. Record the actual version in the evidence record. If the acceptance command still fails, escalate again -- do NOT switch to an absolute target to make it pass.

## Governing constraints

`docs/plans/CAMPAIGN-CONSTRAINTS.md` applies in full: the nine standing rules, amendment A12,
the commit trailers, the branching and pull-request convention, the mandatory audit roster of
one hunter, two independent skinners and one tracker, and the settled decisions that are never
reopened.

## Definition of done

Every acceptance command in this card's plan exits 0 when re-run in a fresh shell (except any
the plan explicitly names as an inspection rather than a gate), both skinners report no
confirmed gating finding, the tracker approves the diff, the pull request into `main` is
merged, and `docs/PLAN.md` carries this card's task ticked.
