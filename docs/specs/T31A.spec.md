# Spec — card `T31A`

**Campaign:** wiki-harness-migration | **Phase:** P5 — Second-wiki smoke | **Depends on:** nothing

## What this card delivers

T31A — init.py: absolute hook paths in dry_run_hooks; cut v1.0.1.

This spec is a thin pointer, not a restatement. The requirement contract for the task below
is its brief, listed with the task. Read the brief in full, including any trailing section
headed "Warchief amendment", before writing a line of code.

## Tasks in this card

1. **T31A** — T31A — init.py: absolute hook paths in dry_run_hooks; cut v1.0.1
   - Brief (the real contract): `/Users/hip/repo/wiki-harness-analysis/plan/briefs/T31A.md`
   - Task branch: `task/T31A-init-relative-target`
   - Campaign task number: 1 of 5

## Evidence record

This card needs no A12 evidence record: it changes library code and cuts a release, which is already a real pull request into `main`.

## Scope fence

- Only the task listed above may produce a change. Nothing else in the repository is touched.
- `/Users/hip/repo/ogp-wiki`, the live wiki, is never written to. Not one byte.
- No CI workflow file is created, in this card or any other.
- Build nothing beyond the brief; escalate an ambiguity instead of guessing at it.

INSERTED CARD, Shaman ruling R4 of 2026-09-02. It is not in plan-v3 §7: T31's own smoke run found a real, deterministic, user-facing defect in the released v1.0.0 and correctly refused to fix it out of scope or paper over it. Inserting a corrective card mid-plan is established practice here -- T08b, C3-1, T12b and T16B were all inserted this way. Keep the fix MINIMAL: resolve the two program paths in `dry_run_hooks` to absolute and leave `cwd=target` alone. Do not redesign step 13 and do not normalise `target` globally at the CLI boundary as a 'better' fix -- a wider change has a wider blast radius across a released entry point. `scripts/` is schema-locked and this plan does NOT declare allowsSchemaChange; if you think a `scripts/` change is required, STOP and report upward. Never retag, move, or rewrite `v1.0.0`.

## Governing constraints

`docs/plans/CAMPAIGN-CONSTRAINTS.md` applies in full: the nine standing rules, amendment A12,
the commit trailers, the branching and pull-request convention, the mandatory audit roster of
one hunter, two independent skinners and one tracker, and the settled decisions that are never
reopened.

## Definition of done

Every acceptance command in this card's plan exits 0 when re-run in a fresh shell (except any
the plan explicitly names as an inspection rather than a gate), both skinners report no
confirmed gating finding, the tracker approves the diff, the pull request into `main` is
merged, and `docs/PLAN.md` carries this card's task ticked.
